name: Update Dashboard

on:
  schedule:
    # Every 30 minutes
    - cron: "*/30 * * * *"
  workflow_dispatch:  # Allow manual trigger from GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Needed to commit index.html back to repo

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate dashboard
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          ADS_PRUNER_HEALTH_URL: ${{ secrets.ADS_PRUNER_HEALTH_URL }}
          ROUTE_OPTIMIZER_HEALTH_URL: ${{ secrets.ROUTE_OPTIMIZER_HEALTH_URL }}
          BOOKING_FUNNEL_URL: ${{ secrets.BOOKING_FUNNEL_URL }}
          MAKE_BOOKING_SCENARIO_ID: ${{ secrets.MAKE_BOOKING_SCENARIO_ID }}
          MAKE_BOOKING_LOGS_URL: ${{ secrets.MAKE_BOOKING_LOGS_URL }}
          MAKE_ESTIMATE_SCENARIO_ID: ${{ secrets.MAKE_ESTIMATE_SCENARIO_ID }}
          MAKE_ESTIMATE_LOGS_URL: ${{ secrets.MAKE_ESTIMATE_LOGS_URL }}
          MAKE_SOCIAL_SCENARIO_ID: ${{ secrets.MAKE_SOCIAL_SCENARIO_ID }}
          MAKE_SOCIAL_LOGS_URL: ${{ secrets.MAKE_SOCIAL_LOGS_URL }}
        run: python generate_dashboard.py

      - name: Commit and push index.html
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          # Only commit if index.html actually changed
          git diff --staged --quiet || git commit -m "chore: update dashboard $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
